{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "functions": {
    "src/orchestrator.py": {
      "maxDuration": 10
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "src/orchestrator.py"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}